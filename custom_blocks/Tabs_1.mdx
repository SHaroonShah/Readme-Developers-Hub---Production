---
name: Tabs_1
---
import React, { useState } from 'react';

export const Tab = ({ children }) => {
  return <div>{children}</div>;
};

export const Tabs = ({ children, tabBorderGap = 4, activeBorderThickness = 3, paddingBottomGap = 10, activeBorderColor='#ca8a04', inactiveBorderColor='#e5e7eb' }) => {
  const [activeTab, setActiveTab] = useState(0);

  return (
    <div>
      <header>
        <nav className="border-b-2 border-white-200 dark:border-gray-600 mb-5"
style={{ paddingBotto: tabBorderGap, paddingBottomGap, borderBottom: `1px solid {#e5e7eb}`}}>
          {children?.map((tab, index) => {
            const isActive = activeTab === index;
            return (
              <button
                key={tab.props.title}
                className={`
                 bg border-1 font-semibold text-sm cursor-pointer mr-4 pb-2
                 text-black hover:text-yellow-500 dark:hover:text-yellow-700 rounded
                 ${isActive && 'text-white-700 -mb-1 border-b-1 border-gray-600 dark:text-yellow-600 dark:border-yellow-500'}`}
                onClick={() => setActiveTab(index)}

									className={`
                  relative mr-4 pb-2 font-semibold text-sm cursor-pointer
                  text-black hover:text-yellow-500 dark:hover:text-yellow-700 
                  ${isActive ? 'dark:text-yellow-600' : ''}
                `}

              >
                {tab.props.icon && (
                 <i className={`fa-duotone fa-solid ${tab.props.icon} text-${tab.props.iconColor} mr-2`}></i>
                )}
                {tab.props.title}

{isActive && (
                  <span
                    className="absolute left-0 right-0"
                    style={{
                      bottom: -tabBorderGap,
                      height: `${activeBorderThickness}px`,
                      backgroundColor: activeBorderColor,
                    }}
                    />
)}


              </button>
           )}
          )}
        </nav>
      </header>
      <section>{children && children[activeTab]}</section>
    </div>
  );
};

<Tabs>
  <Tab title="First Tab" icon="fa-clock" iconColor="blue-500">
    Welcome to the content that you can only see inside the first Tab.
  </Tab>
  <Tab title="Second Tab" icon="fa-star" iconColor="blue-500">
    This is content that you can only see inside the second Tab.
  </Tab>
  <Tab title="Third Tab" icon="fa-heart" iconColor="blue-500">
    This is content that you can only see inside the third Tab.
  </Tab>
</Tabs>